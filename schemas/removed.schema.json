{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sysadmindoc.github.io/CoolSites/schemas/removed.schema.json",
  "title": "CoolSites removed entry",
  "type": "object",
  "required": ["url", "removedAt", "reason"],
  "additionalProperties": false,
  "properties": {
    "url": {
      "type": "string",
      "format": "uri",
      "description": "The address that was listed. Lint rejects re-adding it under either url or repository, comparing host without www and path without a trailing slash, so a protocol swap, a www prefix or a tracking parameter does not slip past."
    },
    "name": { "type": "string", "minLength": 1 },
    "removedAt": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
    "reason": {
      "type": "string",
      "minLength": 12,
      "description": "Why it went. A removal without one is indistinguishable from an accident, and the next contributor to propose it has nothing to read."
    }
  }
}
